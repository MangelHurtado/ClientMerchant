import ClientDelivery from "@/common/components/ClientComponent/Delivery"

export default function ClientsPage({ searchParams }: { searchParams: any }) {
  return <ClientDelivery searchParams={searchParams} />
}
